import os from 'os';

// nice if you want memory / cpu use

// userInfo()
console.log(os.userInfo());

// totalMem()
console.log(os.totalmem());

// freemem()
console.log(os.freemem);

// cpus()
console.log(os.cpus());